[pytest]
testpaths = tests
addopts = -ra
markers =
    slow: long-running tests (multiprocessing, end-to-end training); deselect with -m "not slow"
    network: tests that require internet access (none by default; downloads are never exercised)
filterwarnings =
    ignore::DeprecationWarning:torchvision
